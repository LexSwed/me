<code {...Astro.props}><slot /></code>

<style>
  code:not(pre *) {
    border-radius: var(--radius-sm);
    background-color: hsl(from var(--color-on-background) h s l / 0.1);
    padding: 0.1em 0.2em;
    margin: -0.1em 0;
    font-family: var(--font-mono);
    font-size: 0.9em;
  }
</style>
