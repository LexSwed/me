---
const props = Astro.props;
console.log("code", props);
/**
 * There's no better way to know if the code is inline or multiline :
 */
const isMultiLine = Boolean(props.class);
---

<pre
  tabindex={props.tabindex}
  style={props.style}
  class:list={[
    props.class,
    "code ring-on-background rounded-lg p-4 font-mono text-sm outline-none focus:ring-2",
  ]}><slot /></pre>

<style>
  .code {
    overflow: overlay;
    font-size: 1em;
  }
</style>
