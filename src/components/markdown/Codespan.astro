---
const props = Astro.props;
console.log("codespan", props);
---

<code
  {...props}
  class:list={[
    props.class,
    "bg-on-background/10 rounded-md px-[0.2em] py-[0.1em] font-mono text-[0.8em]",
  ]}><slot /></code
>
