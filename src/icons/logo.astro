---
export type Props = astroHTML.JSX.SVGAttributes;

const { "class": className, "class:list": classList, ...props } = Astro.props;
---

<img
  src="/logo-inner.svg"
  aria-hidden="true"
  class:list={[classList, "h-10 w-10 rounded-xl bg-on-primary p-2", className]}
  {...props}
/>
